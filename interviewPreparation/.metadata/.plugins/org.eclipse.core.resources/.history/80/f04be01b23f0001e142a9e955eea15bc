package com.sisters;

public class edaSister {
	// instance --> Declared inside the class but outside the method.(global variable )
	// local variable ==> Declared Outside the class but inside the method
    // static variable --> public only (fixed)
}
